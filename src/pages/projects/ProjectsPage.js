import React from 'react';
import PropTypes from 'prop-types';

import Wrapper from '../Wrapper';

const ProjectsPage = (props) => {
  return (
    <Wrapper>
      <div>The Projects Page</div>
    </Wrapper>
  );
};

ProjectsPage.propTypes = {};

export default ProjectsPage;
